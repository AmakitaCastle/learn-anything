# 安全说明

模型／语音密钥只放在忽略的 `.env.local`。不要通过 Issue、PR、截图、课程材料或生成报告发送凭证。

终端播放器只绑定 `127.0.0.1`，提供当前课程及打包前端的封闭文件列表。它不是公网服务，不能直接作为在线部署接口。供应商根地址由本机用户可信配置；端点校验不是任意不可信 URL 的通用 SSRF 防护。

课程只能使用受约束的 LessonDraft／LessonSpec 字段与已注册动画，不执行模型输出的任意代码。Schema 校验不保证知识正确或参考资料可信，使用前应人工检查。

发现可能涉及密钥泄露或代码执行的问题，请勿在公开 Issue 中附利用细节或敏感数据。通过发布仓库的 GitHub 私密漏洞报告渠道联系维护者；如果该渠道未启用，请先创建不含细节的 Issue，请维护者提供私密联系方式。
