# learnAnything：从动态讲课 Demo 到开源项目

## 1. 项目定位

learnAnything 的核心不是管理课程、资料或待办事项，而是把静态知识转化成一段能够吸引并维持注意力的动态讲解。

核心体验由三部分组成：

- 语音讲解：控制讲解节奏，让学习者沿着一条清晰的思路前进。
- 动态板书：逐步留下定义、公式、关系和结论，形成可回看的外部记忆。
- 概念动画：展示文字难以表达的变化、结构、空间关系和因果过程。

三者共享同一个课堂时间轴：

```text
Classroom(t)
  = Voice(t)
  + BoardState(t)
  + AnimationState(t)
  + AttentionState(t)
```

语音是主时钟。播放、暂停、倍速、跳转和追问后继续，都必须从同一个确定性的课堂状态恢复。

## 2. 产品原则

1. 先证明一堂课值得看完，再加入 AI 自动生成。
2. 动画必须解释变化，不能只是装饰。
3. 板书负责留下结论，动画负责展示原因，语音负责组织节奏。
4. 模型生成受约束的课堂描述，不生成任意 HTML、JavaScript 或动画代码。
5. 每堂课都可以暂停、后退、跳转、重播和恢复。
6. 不支持某类专用动画时，降级为通用图示或板书，不生成不可靠的科学模拟。
7. 核心协议与模型、语音服务和资料来源解耦。

## 3. 第一阶段：Golden Demo

### 目标

手工制作一堂 60～90 秒的动态课程，证明语音、板书和动画能够形成有吸引力的学习体验。

推荐首个主题：

> 二分查找为什么每一步都能排除一半？

### 范围

- 使用手工编写的 `lesson.json`。
- 使用预先生成的固定语音。
- 展示一个数组和目标值。
- 动画呈现 `low`、`mid`、`high` 的变化。
- 板书逐步留下算法不变量。
- 预测问题作为旁白中的思考提示，保持连续播放，不自动暂停或弹出选择题；学习者可以随时手动暂停。
- 提供播放、暂停、后退十秒、拖动进度、倍速和重新开始。

### 暂不包含

- 文件上传和 RAG。
- AI 自动备课。
- 登录、账户和数据库。
- 学习计划与长期学习画像。
- 数字人、口型和复杂 3D。
- LMS、移动端和多人课堂。

### 验收标准

- 语音、板书和动画不会漂移。
- 暂停语音时，所有轨道一起暂停。
- 预测节点不触发自动暂停，旁白按原时间轴继续进入答案；手动暂停后可从同一状态继续。
- 跳转到任意时间后，画面可以恢复为正确状态。
- 重播同一份课程时结果一致。
- 不依赖字幕也能理解讲解。
- 关闭声音后，板书仍保留清晰的知识结构。
- 动画承担实际解释作用。

### 对应版本

`v0.0.1` — Golden Demo

## 4. 第二阶段：可复用课堂引擎

### 目标

把 Golden Demo 中的专用代码拆成独立、可复用和可测试的播放器能力。

### 建议仓库结构

```text
learn-anything/
├── apps/
│   └── demo/
├── packages/
│   ├── lesson-schema/
│   ├── lesson-player/
│   ├── board-renderer/
│   ├── visual-grammars/
│   ├── timeline-compiler/
│   └── provider-adapters/
├── examples/
│   ├── binary-search/
│   ├── fourier-transform/
│   └── blood-circulation/
└── docs/
```

### 核心公共接口

- `LessonSpec`：描述讲解内容、旁白、语义锚点和互动节点。
- `TimelineEvent`：描述编译后的真实时间事件。
- `VisualGrammar`：定义一种概念动画语法及其渲染方式。

### 第一批板书能力

- 文字和短语渐显。
- 数学公式分步出现。
- 箭头、曲线、圈选和下划线绘制。
- 局部高亮。
- 画面分区和确定性布局。
- 从任意时间点重建板书状态。

### 第一批动画语法

- `flow`：流程和信息流动。
- `state-transition`：状态转换。
- `plot`：函数或数据变化。

### 对应版本

`v0.1.0` — Classroom Player

## 5. 第三阶段：语音与课堂编译器

### 目标

把带语义锚点的讲解稿编译为真正可播放的课堂时间轴。

### 编译过程

```text
讲解稿与语义锚点
→ TTS 生成连续语音
→ 获取或对齐词级时间
→ 将语义锚点解析为真实时间
→ 编译 TimelineEvent
→ 播放器执行
```

模型不能直接指定毫秒时间。它只输出类似“说到‘排除左半边’时执行 `discard_left_partition`”的语义关系，编译器根据真实语音确定执行时间。

### 验收标准

- 更换语速后仍可重新编译并保持同步。
- 单个词对齐失败时可以回退到句级时间。
- 音频、对齐结果和编译时间轴可以缓存。
- 编译输出可以离线重复播放。

### 对应版本

`v0.2.0` — Lesson Compiler

## 6. 第四阶段：AI 自动生成课堂

### 目标

让用户输入一个主题，系统自动生成一堂可以预览、修改和播放的课程。

### 分层生成

```text
主题
→ TeachingPlan
→ LessonDraft
→ Schema 校验
→ 语音与时间轴编译
→ 可播放课堂
```

### TeachingPlan 应包含

- 本节课解决的问题。
- 前置知识。
- 教学顺序。
- 可能的误解。
- 应当使用板书还是动画。
- 预测、停顿和检查理解的位置。

### 安全边界

- 只接受符合 Schema 的结构化输出。
- 不执行模型生成的脚本。
- 动画只能调用注册过的 `VisualGrammar`。
- 生成失败时展示校验错误并允许重新生成。
- 发布或分享前允许人工预览和编辑。

### 对应版本

`v0.3.0` — AI Lesson Generation

## 7. 第五阶段：从资料学习

### 目标

支持 PDF、Markdown 和网页，把用户资料转换为带来源引用的动态讲解。

### 核心能力

- 文档解析和结构识别。
- 章节选择与内容分块。
- 基于来源生成 TeachingPlan。
- 每个重要知识主张关联原始来源。
- 点击引用返回原文位置。
- 明确区分来源事实和 AI 补充说明。

### 对应版本

`v0.4.0` — Learn From Sources

## 8. 第六阶段：稳定协议与社区生态

### 目标

让外部项目能够依赖 learnAnything 的课堂协议和播放器，社区能够独立贡献动画、声音和学科包。

### 重点扩展方向

- 新的概念动画语法。
- 数学、物理、生物和计算机学科包。
- 板书视觉主题。
- 中文及其他语言声音。
- AI、TTS 和语音对齐 Provider。
- 示例课程和教学策略。
- 无障碍与减少动态效果模式。

### 对应版本

`v1.0.0` — Stable Lesson Protocol

在 `1.0` 之前允许协议调整；进入 `1.0` 后维护 LessonSpec 和插件接口的兼容性。

## 9. 三个泛化验证案例

| 案例 | 验证能力 |
| --- | --- |
| 二分查找 | 数组、代码和状态变化同步 |
| 傅里叶变换 | 数学公式、曲线和视觉分解 |
| 血液循环 | 空间结构、流程和方向变化 |

如果每个案例都需要重新编写播放器，说明抽象仍然不成立。如果主要工作只是增加新的 `VisualGrammar`，说明项目已经具备开源扩展价值。

## 10. 首批开发任务

1. 定义 `LessonSpec v0`。
2. 实现统一播放时钟。
3. 实现支持 seek 的事件状态归并器。
4. 实现文字、公式、箭头和高亮板书。
5. 实现 `flow` 动画语法。
6. 实现 `state-transition` 动画语法。
7. 实现 `plot` 动画语法。
8. 手工完成二分查找 Golden Demo。
9. 加入 TTS 和语义锚点对齐。
10. 编写“如何新增动画语法”文档。

## 11. 开源发布检查

### 用户能够运行

- 仓库自带一堂不需要 API Key 的示例课。
- 一个命令即可启动。
- README 第一屏直接展示真实讲课 GIF 或视频。
- 本地依赖和平台限制清晰。
- AI、TTS 或对齐服务失败时有可理解的错误提示。

### 开发者能够理解

首次公开前应包含：

```text
README.md
ARCHITECTURE.md
CONTRIBUTING.md
ROADMAP.md
SECURITY.md
CODE_OF_CONDUCT.md
LICENSE
```

### 贡献者能够验证

每个动画语法需要提供：

- 固定输入 fixture。
- 播放状态测试。
- seek、暂停和重播测试。
- 视觉基准图。
- 最小示例课程。

## 12. 开源策略

- 推荐使用 Apache-2.0。
- Golden Demo 可运行后公开 GitHub 仓库。
- 初期使用 DCO，不急于引入复杂 CLA。
- 核心协议保持模型无关和 Provider 可替换。
- 示例资料只使用原创、公共领域或许可清晰的内容。
- 不把任何云端 API 设置为唯一运行方式。

建议对外定位：

> Open-source runtime for turning knowledge into narrated, animated lessons.

## 13. 从 Demo 到开源项目的判断标准

Demo 证明“我们能做出一堂好看的课”。

开源项目还必须证明：

- 新用户能够独立运行。
- 新开发者能够理解架构。
- 贡献者能够增加一种动画而不修改核心播放器。
- 所有示例都能确定性重播。
- 协议、测试、文档和版本发布能够长期维护。

## 14. 下一步

按用户确认的预期，流水线是“LLM 生成文稿材料 → `content-generator` 编译 → `lesson-player` 播放编译产物”。结构上先保留两个可独立构建的业务子项目：`packages/content-generator` 负责 TTS／音频制作／字幕／对齐／课程编译，`packages/lesson-player` 负责完整“语音＋板书＋动画”的播放；LLM 材料生成是上游独立环节，不混入编译或播放。`packages/lesson-schema` 定义编译产物交接协议。先采用同仓 npm workspaces，不创建 Git 子模块或新远程仓库，见[模块架构](./docs/module-architecture.md)。

`v0.1.0` 优先验收播放子项目独立构建、无演示应用依赖和实际播放恢复；内容项目的结构独立不代表第三阶段编译能力或第四阶段 AI 自动备课已全部实现。现有人工讲稿与 TTS 流程继续可用，AI 后端仍待实现。

统一 `LessonDraft 0.1.0` 材料和 `compileLessonDraft()` 通用入口已补齐：旁白、板书、内置动画动作和短语锚点编译成实测时间的 MP3、课程 JSON、VTT。水循环与温度曲线由同一逻辑读取材料；`createLessonArtifacts()` 仍只负责已编译产物的校验／序列化。格式与边界见[通用编译说明](./docs/lesson-draft.md)。AI 材料生成仍属于第四阶段，尚未接入。

用户已确认进入 `v0.1.0` 的课堂引擎抽象阶段。首页随后按用户要求换成水循环；原 Golden Demo 在 `/examples/binary-search` 归档保留，继续作为字体和播放器回归基线。

本轮实现的能力边界与调用说明见[可复用课堂能力](./docs/classroom-capability.md)：公共 `LessonSpec 0.1.0`、统一音频时钟、板书渲染器、注册式数组/流程/状态转换/曲线轨道，以及仅更换数据的展示视角。TTS 合成和语义锚点编译仍是上游能力，不随播放器抽象混入。

现已增加与二分查找无关的水循环课，包含新旁白、板书和纯数据动画视角，见[水循环说明](./docs/water-cycle.md)。后续仍需更多主题的完整教学案例、人工教学质量与低置信度语音核对，以及开源发布材料；水循环示例不等同于傅里叶变换、血液循环或全部学科泛化已通过验收。
