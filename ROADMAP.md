# 路线图

## 第一版：本地 Web + CLI Demo

本版按“LLM 输出 LessonDraft 0.1.0 → compileLessonDraft() → MP3、LessonSpec 0.1.0、VTT → lesson-player”交付。用户可以在本地 Web 工作台完成主题输入、进度查看和课程播放，CLI 继续承担批处理、恢复与导出。共享协议版本保持 0.1.0，首个 Demo 发布标签使用 `v0.1.0-demo`。

- 无密钥自带课程：`npm run demo`。
- 本地 Web 工作台：`npm run web`。
- 主题生成与编译：`npm run lesson -- "主题" --generate`。
- 保存产物、课程重播、语音缓存与编译失败恢复。
- 本地 MP4 导出，支持 16:9／9:16／1:1 比例及带旁白的确定性帧渲染。
- 语音、全文板书、局部重点和注册式动画共享确定性时间轴。
- 文档、许可证、自动检查和 GitHub 源码发布。

首版验收见 [demo-release.md](./docs/demo-release.md)。

## 后续候选

先根据 Demo 使用反馈确定优先级：

1. 多主题生成质量、真实模型成功率与听感评估。
2. 更易使用的配置引导、材料编辑及课程分享。
3. PDF、Markdown、网页解析和可回溯的来源引用。
4. 更多学科动画与复杂公式表达。
5. 互动追问、无障碍及减少动态效果。
6. 稳定协议、独立包发布与社区插件。

早期“Golden Demo → 课堂引擎 → 编译器 → AI 生成”的阶段规划保留在原设计文档；它描述历史拆解，不作为当前首版发布的前置范围。
